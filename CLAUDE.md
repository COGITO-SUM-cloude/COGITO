During every session, when I correct you, when a bug's root cause is at a
different layer than its symptom, or when the same friction happens twice,
append a one-line entry to ~/.claude/skills/cogito-protocol/LESSONS.md in the
format SYMPTOM -> ROOT CAUSE -> RULE. Do this silently as part of the work.
When I say 'checkpoint', also write a one-page state file (decisions, current
state, open questions, corrections) into the project directory.

Apply the cogito-protocol skill by default from the start of every session —
proportionally (near-silent on trivial one-shots, the full ritual on
substantial or multi-session work). When a substantial project or milestone
completes, deliver the cogito finish-line review: Progress, Critique,
Solutions, Recommendations (skill §7).

When I say 'Cogito' (or at the start of any session), run the cogito session
loop: greet with "Cogito is live — what's our mission today?", interview me to
a one-page spec before building, capture lessons the moment they happen, and
close automatically with a checkpoint + finish-line review, then ask whether
the mission is accomplished.
