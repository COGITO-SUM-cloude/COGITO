# ACTIVE MISSION — resume pointer
_Kept short on purpose: this file is `cat` into every session, so it costs tokens every turn._

When the user says "Cogito": greet in one line, confirm, continue. Don't re-interview.

## Mission — build the personalised AI TEACHER (structure-first)
Build the teacher tailored to the USER first (he's the tester; needs no VR headset), get the
"golden structure" right, prove it, then reuse it for his **brother** (swap learner + child
scaffolding) and add a **VR/3D** presentation layer LATER. The user accepts a multi-year timeline
and wants to start now. **30-day Claude Pro window — build aggressively; do NOT token-optimize
until the user says he's limited.**

Golden structure (presentation- & learner-agnostic): **Learner Profile · Adaptive Lesson Loop ·
Progress Memory · Content Engine · (Presentation layer = text now, VR later).** Cogito already
seeds it — `skills/adaptive-learning` is the Loop, `docs/learning/log.md` is the Progress Memory.
Full plan + architecture: **`docs/checkpoints/2026-06-15-ai-teacher-pivot.md`** (read this first).

## Start next session HERE
1. Convene the **council** (fresh window) on the AI-teacher architecture + build order.
2. Build **v0 file-based teacher tailored to the user** (his subject ≈ neuroscience/cognition),
   reusing adaptive-learning + the learning log — a real runnable loop, not a doc.
3. In parallel (the Pro-window jobs): finish ChatGPT/DeepSeek **data export → verify → clean into
   Cogito**; **one** capped cognition-levers research file; the **brother's Week 1** once his
   `docs/teaching/01-placement-check.md` results come back.

## Tools/decisions already in place
- Council (`skills/cogito-council` + `.claude/agents/cogito-judge.md`); phase 2 = real OpenRouter
  Fusion when the user has a key. Lean loading (index mode, ~1.4k tok/turn; `scripts/cogito-budget.sh`).
- Brother: he gets school help already; possible-dyslexia screening is the school's job, not AI's.
  The adult adaptive-learning skill does NOT fit a child — his teaching is new, scaffolded work.

## Prior mission — DONE
10-item Cogito upgrade roadmap (`docs/projects/06-cogito-upgrade-roadmap.md`), 2026-06-15.

_(Hard-won guardrails ride as `#critical` ledger lessons — always loaded — so not repeated here.)_
