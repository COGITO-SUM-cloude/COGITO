# Cogito — docs

Room to grow. This directory is where deeper documentation lands as Cogito
matures beyond the protocol itself.

## Candidate next steps

- **Worked examples** — annotated session transcripts showing the loop in
  practice (interview → red-team → increments → finish-line review).
- **Lessons → rules pipeline** — how an accumulated `LESSONS.md` gets distilled
  back into `SKILL.md` once ~5 lessons collect (skill §4b).
- **Adoption guide** — installing Cogito globally vs. per-repo, and wiring the
  `--global-hook` so the protocol loads in every session, every repo.
- **Companion skills** — the relationship to a systems-thinking skill (sources
  of truth, layers, downstream consumers) referenced in `SKILL.md`.

Contributions welcome — Cogito is meant to be shared.
